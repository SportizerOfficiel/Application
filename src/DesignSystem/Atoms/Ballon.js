import { IconBallAmericanFootball, IconBallFootball, IconBalloon } from '@tabler/icons-react';
import React from 'react';

const Ballon = (props) => {
    return (
        <IconBallFootball {...props}></IconBallFootball>
    );
};

export default Ballon;